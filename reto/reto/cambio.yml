info:
  name: cambio
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/cambio
  body:
    type: json
    data: |-
      {
        "clienteId":"test_124",
        "monto": 2000,
        "tasa": 0,
        "monedaOrigen": "EUR",
        "monedaDestino": "GBP"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
